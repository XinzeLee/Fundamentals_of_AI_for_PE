# Python dependencies for Fundamentals_of_AI_for_PE tutorial notebooks.
# System tools (e.g. LTspice, PLECS, MATLAB) are not installable via pip.
# For macOS users, when using `uv`, first install OpenMP required by XGBoost: `brew install libomp`

numpy>=1.24,<2.0
matplotlib
seaborn
pandas
scipy
scikit-learn
xgboost
torch
pyswarms
pygad
scikit-image
imageio
paretoset
onnx
onnxruntime
fuzzytree
PyLTSpice
func-timeout
tqdm
